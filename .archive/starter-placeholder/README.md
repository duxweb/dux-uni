# starter

This app shell is the intended real-host integration target for `@duxweb/uni`.

Recommended next step:

1. initialize a standard `uni-app` project shape here
2. add `vite-plugin-uni-pages`
3. register `pinia`
4. wire page meta and guards through `@duxweb/uni`
