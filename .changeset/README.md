This directory is managed by Changesets.

- Run `pnpm changeset` to create a release note.
- Run `pnpm version-packages` to bump versions and sync template metadata.
- Run `pnpm release` to publish public packages.
